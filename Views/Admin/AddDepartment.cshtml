@model hospitalMS.Models.Department

@{
    ViewBag.Title = "Add Department";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<main class="dashboard-content">
    <div class="container-fluid px-3 px-lg-4 py-4">

        <div class="page-heading">
            <div class="page-heading-copy">
                <span class="page-icon"><i class="bi bi-building"></i></span>
                <div>
                    <p class="eyebrow mb-1">Management</p>
                    <h1 class="h3 mb-1">Add Department</h1>
                    <p class="text-muted mb-0">Fill department information.</p>
                </div>
            </div>
        </div>

        <section class="row g-3">
            <div class="col-12 col-xl-8">

                <form action="@Url.Action("AddDepartment","Admin")" method="post">

                    <div class="panel">

                        <div class="panel-header">
                            <div>
                                <h2 class="h5 mb-1 section-title">
                                    <i class="bi bi-plus-circle"></i>
                                    <span>Add Department</span>
                                </h2>
                                <p class="text-muted mb-0">Enter department details.</p>
                            </div>
                        </div>

                        <div class="row g-3">

                            <div class="col-12">
                                <label class="form-label">Department Name</label>

                                @Html.TextBoxFor(x => x.Name, new
                                {
                                    @class = "form-control",
                                    placeholder = "Enter Department Name"
                                })

                                @Html.ValidationMessageFor(x => x.Name, "", new { @class = "text-danger" })
                            </div>

                        </div>

                        <div class="d-flex justify-content-end mt-4">
                            <button class="btn btn-primary" type="submit">
                                <i class="bi bi-check-circle"></i>
                                Save Department
                            </button>
                        </div>

                    </div>

                </form>

            </div>
        </section>

    </div>
</main>

